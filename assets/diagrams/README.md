# Diagram placeholders

Add the following image files here for full README and docs integration:

| File | Description |
|------|-------------|
| `architecture.png` | System architecture — high-level components (API, services, DB) |
| `speech-pipeline.png` | Speech processing pipeline — ASR → NLP → Report → TTS |
| `api-workflow.png` | API workflow — auth, chat, and report request flow |

You can use any tool (draw.io, Mermaid export, Excalidraw, etc.) to create these diagrams. The main README and `docs/ARCHITECTURE.md` reference these paths.
